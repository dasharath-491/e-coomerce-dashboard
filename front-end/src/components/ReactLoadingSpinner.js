import React from "react";
import "../App.css"

const ReactLoadingSpinner = () => {
    return (
        <div className="spinner-container">
            <div className="loading-spinner"></div>
        </div>
    );
};

export default ReactLoadingSpinner;
